<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khaepah.khaepah_19090017">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Khaepah_19090017">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DashboardActivity" />
        <activity android:name=".DokterActivity" />
        <activity android:name=".AlatKesehatanActivity" />
        <activity android:name=".ObatActivity" />
        <activity android:name=".PasienActivity" />
        <activity android:name=".PenyakitActivity" />
        <activity android:name=".RumahSakitActivity" />
    </application>

</manifest>